---
name: industrial-control-cpp-style
description: "为 C++17 工控、机器人控制、运动规划、硬件管理和实时运行时代码提供统一的工程编码约束。默认关注业务流程线性、所有权清晰、故障可定位、控制周期可分析和人工可读性；需要专题审查时使用配套的 review、audit 或 debt skill。"
---

# 工控 C++17 核心规范

这是本规范集合的默认入口。它约束“如何思考和产出代码”；专题 skill 负责一次性的审查、仓库扫描和债务汇总。

## 使用边界

只在 C++17 工控、机器人、运动控制、硬件管理、Runtime、Application ABI、驱动适配和控制周期代码中使用。普通业务软件不因使用 C++ 就自动套用全部规则。

发生冲突时优先级如下：

1. 安全语义和行为正确性；
2. 所有权、生命周期和线程所有权明确；
3. 控制周期边界、资源上界和状态转换可分析；
4. 业务职责、故障来源和提交点清楚；
5. 代码简洁、注释适量、公共表面稳定。

## 固定工作协议

修改前阅读目标模块的公开头文件、实现、同模块测试和事实文档；沿真实调用链确认输入、输出、单位、线程和状态，不把历史设计稿当作现行契约。

实现前先写清楚：

- 正确输入形式、所有权、单位和有效范围；
- 本模块真正拥有的业务不变量；
- 正常拒绝、外部不可用、内部故障和安全禁止的区别；
- 候选结果在哪里产生，哪个位置真正提交副作用；
- 顶层如何看到模块、阶段、操作和根因。

## 六条始终生效的硬约束

- `[MUST NOT]` 不能把内部故障、未接受的输出或无效输入伪装成成功。
- `[MUST NOT]` 没有明确业务策略时，不能静默使用旧值、零值、默认值、空结果或备用路径继续运行。
- `[MUST NOT]` 控制周期不能引入未经审查的分配、阻塞、锁竞争、同步 I/O、字符串格式化、异常传播或日志回调。
- `[MUST NOT]` 在硬件/外部操作被接受前发布完成状态、持久意图或“本周期已执行”的快照。
- `[MUST NOT]` 上层不能清除或覆盖底层故障的原始错误码、阶段和根因，只能补充上下文。
- `[MUST NOT]` 多线程不能无约束地访问同一份含可变工作区的对象；必须有线程所有权或可解释的同步协议。

这些硬约束只保留跨项目、几乎没有合法例外且违反后会改变行为安全性的规则。命名、拆分、注释密度和具体容器选择默认是 `[SHOULD]`，不要机械升级为禁止项。

## 固定决策阶梯

遇到新增 guard、默认值、fallback、重试或异常捕获时，按顺序判断：

```text
1. 这是真实业务情况吗？不是：删除假想防御。
2. 输入来自不可信边界吗？是：在边界校验一次。
3. 这是本模块拥有的业务不变量吗？是：本模块校验。
4. 失败是调用者可修正的输入问题吗？是：明确拒绝。
5. 是外部资源不可用吗？是：进入可见的 unavailable/degraded/safe 状态。
6. 是内部不变量破坏吗？是：保留根因，硬失败，停止依赖该结果的流程。
7. 有明确 fallback 策略吗？没有：失败；有：公开原因、退出条件并测试。
8. 顶层能定位模块、阶段、操作和根因吗？不能：补充故障投影。
```

一个模块只对契约规定的正确输入负责，并只校验自己拥有的业务语义。信任上游契约不等于取消本模块的不变量检查；不属于本模块职责的问题应报告并终止当前流程，而不是擅自修正。

## 代码骨架

### 业务流程

非平凡业务函数优先保持如下可读顺序：

```text
输入/前置条件
→ 状态与安全判断
→ 核心计算或动作
→ 唯一提交点
→ 状态、快照和事件发布
```

主流程中必须能看见关键安全门、不可逆动作、失败出口和提交点。helper 使用业务动词命名，如 `readFeedback`、`evaluateSafety`、`prepareOutput`、`publishAndCommit`；不要用 `process`、`handle`、`update` 隐藏因果关系。函数长度不是拆分充分条件。

### C++17

- 使用项目既有缩进、大括号、命名空间和头文件约定；类型用 `PascalCase`，函数用 `camelCase`，编译期常量用 `kPascalCase`。
- 小型配置、命令、快照、ID 和固定容量数据优先使用值语义、`std::array`、`enum class` 和命名类型。
- `std::unique_ptr` 表示拥有；引用表示调用期间必然存在；`std::optional` 只表示正常的“可能没有值”；非拥有裸指针必须显式说明生命周期。
- 用 `std::variant` 表达互斥输入，并在一个边界完成类型分派；不要为每种输入复制平行入口。
- 让函数名准确表达副作用，区分 `read`、`peek`、`consume`、`publish`、`request`、`reset` 和 `recover`。
- 不为形式上的现代化进行无关的全局重命名、格式化或抽象重构。

### 注释

公开接口记录输入、输出、单位、所有权、线程/阶段、实时性、失败和副作用契约；实现只说明业务阶段、执行顺序、提交点和非直观原因。非平凡函数按实际处理阶段分区，但不逐行复述代码，也不写“处理数据”“更新状态”之类空标题。

控制周期注释必须如实描述资源限制；未测量时不得声称硬实时。代码行为变化时同步更新契约注释。

### 状态、故障和实时性

- 核心状态由一个模块拥有；跨模块通过命令、查询或快照访问。
- 互斥生命周期状态用 `enum class`，独立观测事实分开表达；不要让一个 `bool` 同时表示请求、事实和最终许可。
- 失败路径明确对象状态；区分拒绝、不可用、故障、抑制和恢复。
- 控制周期的循环次数、队列访问、数值迭代和失败策略必须有界，或明确标记为尚未验证的软实时路径。
- 构造/初始化阶段可以解析配置、分配和抛出异常；控制周期默认不做这些事。

## 按需加载的专题

只读取当前任务需要的专题，不要默认加载整个资料库：

- 业务流程、线性组织、唯一提交点：阅读 [industrial-control-flow-patterns.md](references/industrial-control-flow-patterns.md)。
- 契约边界、错误分类、故障传播和恢复闭环：阅读 [industrial-control-fault-system.md](references/industrial-control-fault-system.md)。
- 审查 guard、fallback、catch、默认值、过度拆分或注释噪声：阅读 [industrial-control-anti-patterns.md](references/industrial-control-anti-patterns.md)。
- 需要当前 diff 的短报告：使用 `industrial-control-cpp-review`。
- 需要全仓库扫描：使用 `industrial-control-cpp-audit`。
- 需要汇总契约债务和故障债务：使用 `industrial-control-cpp-debt`。
- 需要查看入口和输出格式：使用 `industrial-control-cpp-help`。

## 交付前最小自查

确认：输入责任没有重复或越界；业务流程从上到下连续；安全门先于副作用；存在唯一提交点；失败保留根因并能被顶层定位；控制周期没有未经审查的资源行为；测试覆盖拒绝、故障、恢复和关键边界；实际验证命令和未验证限制已说明。

不要输出泛化的长篇风格宣言。交付时说明行为变化、修改入口、实际验证和遗留的契约/故障债务即可。
