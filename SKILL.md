---
name: industrial-control-cpp-style
description: "为 C++17 工控、机器人控制、运动规划、硬件管理和实时运行时代码提供统一的工程编码约束。用于新增、修改、重构或审查 Core、Hardware Manager、Runtime、Application、驱动适配和控制周期代码，重点约束所有权、状态、线程阶段、错误处理、可观测性、模块边界和人工可读性。"
---

# C++17 工控代码产出规范

## 目标

生成具有工业控制工程师习惯的 C++17 代码：行为明确，所有权清楚，状态可追踪，模块边界稳定，控制周期可分析，失败后可安全收敛，普通工程师能够低负担地阅读和维护。

将 `references/motionlib-v2-style.md` 作为本项目的正面风格参考。复用其设计思想和命名习惯，不机械复制其中已经记录的历史限制。

## 优先级

发生冲突时按以下顺序决策：

1. 安全语义和行为正确性；
2. 所有权、生命周期和线程所有权明确；
3. 控制周期的边界、上界和资源使用可分析；
4. 模块职责单一，接口和状态易于理解；
5. 可观测性、测试性和实现简洁性。

不要为了形式上的风格统一破坏已有行为、协议兼容性或安全策略。

## 工作协议

修改代码前，先阅读目标模块的公开头文件、实现、同模块测试和当前事实文档。将历史设计稿视为背景，不将其直接当作当前 API 契约。

实现前明确以下内容：

- 输入、输出、单位、有效范围和默认值；
- 对象由谁创建、拥有、使用和销毁；
- 哪个线程或执行阶段拥有该对象；
- 状态不变量和允许的状态转换；
- 失败类型、失败后的对象状态和安全输出；
- 是否允许分配、锁、阻塞、I/O、异常和日志；
- 应由哪个快照、诊断对象或事件记录对外解释行为。

优先扩展已有类型、状态和诊断体系。只有在现有模型无法表达新行为时，才引入新的公共类型。

## 规则等级

使用以下标签表达约束：

- `[MUST]`：必须满足；违反即停止并修正；
- `[MUST NOT]`：明确禁止；
- `[SHOULD]`：默认采用，偏离时说明原因；
- `[MAY]`：在满足不变量和阶段约束时允许采用。

每条硬规则都必须针对行为或不变量，而不是只针对某个语法形状。

## C++17 基础风格

- `[MUST]` 使用四空格缩进、`#pragma once`、项目既有的大括号风格和命名空间层级。
- `[MUST]` 类型使用 `PascalCase`，函数使用 `camelCase`，编译期常量使用 `kPascalCase`，私有成员使用 `snake_case_`。
- `[MUST]` 布尔成员使用能够表达事实或意图的前缀，例如 `is_`、`has_`、`can_`、`should_` 或 `enable_`。
- `[SHOULD]` 使用完整、稳定的领域术语；只对项目已约定的缩写使用 `IK`、`FK`、`HM` 等缩写。
- `[MUST]` 让函数名准确表达副作用。区分 `read`、`peek`、`consume`、`publish`、`request`、`reset` 和 `recover`，不要用含义模糊的 `process`、`handle` 或 `update` 掩盖不同语义。
- `[SHOULD]` 让头文件依赖最小化；将实现细节放入 `.cpp` 或 PImpl，不为避免一次前置声明而扩大公共依赖。
- `[MUST NOT]` 为了“看起来现代”而进行无关的全局重命名、格式化或抽象重构。

## 类型、值语义和所有权

- `[MUST]` 对小型配置、命令、快照、ID 和固定容量数据优先使用值语义、结构体、`std::array` 和 `enum class`。
- `[MUST]` 用 `enum class` 表达互斥的状态、模式、阶段或结果；多个独立事实可以分别使用 `bool`。
- `[SHOULD]` 用 `std::variant` 表达互斥的输入类型或内部路径，并在一个位置完成分派。
- `[MUST]` 用 `std::unique_ptr` 表达拥有关系，用引用表达调用期间必然存在的依赖，用 `std::optional` 表达正常的“可能没有值”。
- `[MAY]` 使用非拥有裸指针表达外部拥有的依赖、PImpl、共享内存映射或操作系统句柄。必须在接口、成员注释或所属文档中说明生命周期和所有权；不得让裸指针隐式表示拥有关系。
- `[MUST NOT]` 用裸指针承担隐式释放责任，也不得仅通过“构造后应该有效”的约定掩盖可为空依赖。
- `[SHOULD]` 对跨模块的 ID、单位、轴数量、分支和错误码使用命名类型或枚举，避免用裸整数传递不同语义。
- `[MUST]` 不把 `std::array` 的容量当成有效元素数量；同时传递或存储有效数量，并检查其范围。

## 生命周期和错误处理

- `[MUST]` 构造函数建立对象不变量；配置错误、无法建立对象的错误可以通过异常报告。
- `[MUST]` 运行期操作明确返回成功、拒绝、失败或当前状态，不得设置错误后继续伪装成功。
- `[MUST]` 每个失败路径都说明对象进入的状态；不得留下调用者无法判断的半初始化对象。
- `[SHOULD]` 对可恢复的值操作提供清晰的失败结果；对重复使用的跨模块模式才引入项目统一的 `Result<T, E>`，不要为一次性接口凭空发明 Either 层。
- `[MUST]` 仅使用 `std::optional` 表示缺少值，不用它隐藏错误原因。
- `[MUST]` 只有在操作确实不会抛出且能够维持不变量时才标记 `noexcept`。
- `[MUST]` 对可逆的内存或值操作尽量提供强异常保证；对不可逆的硬件操作，改为发布安全输出、记录明确故障并提供显式恢复路径，不承诺虚假的回滚。
- `[MUST NOT]` 在控制周期中依赖异常传播、动态错误字符串构造或未限定的错误恢复循环。

## 状态和行为

- `[MUST]` 让一个模块拥有其核心状态；其他模块通过命令、查询或快照访问，不得各自复制一套状态机。
- `[MUST]` 集中定义状态转换和非法转换的处理；不得在多个函数中直接修改同一核心状态而不经过转换规则。
- `[MUST]` 将“互斥生命周期状态”和“独立观测事实”分开建模。不要因为字段数量多就机械合并为一个枚举。
- `[SHOULD]` 将命令、当前状态、诊断和输出许可分开表达；不要让一个 `bool` 同时承担请求、当前事实和最终许可三种语义。
- `[MUST]` 让快照成为对外状态的单一来源。日志可以解释事件，但不能取代状态快照或诊断契约。

## 线程、同步和控制阶段

为每个函数标注或明确其所属阶段：

```text
初始化：允许配置解析、文件 I/O、分配、模型加载和异常。
规划/输入：允许受控的算法计算和有限分配；明确队列容量与拒绝策略。
控制周期：默认禁止未审查的分配、锁、阻塞、I/O、字符串构造和日志。
诊断/管理：允许字符串、格式化、较慢查询和详细日志。
```

- `[MUST]` 为共享对象定义线程所有权或同步协议；“队列存在”不等于“线程安全”。
- `[MUST]` 同一份内存及其不变量只能有一套可解释的同步协议。原子发布索引、序列锁、双缓冲和互斥保护可以组合，但必须说明读写顺序。
- `[MUST NOT]` 让多个线程无约束地调用含有可变工作区的 solver、Runtime 或 Pipeline。
- `[SHOULD]` 将原子变量用于独立标志、计数器或发布索引，将互斥锁用于非实时复合数据；不要用原子与锁互相掩盖未定义的状态关系。
- `[MUST]` 控制周期中的循环次数、队列访问、数值迭代和失败策略具有明确上界，或明确标注为尚未验证的软实时路径。
- `[MUST]` 新增 `sample()`、servo、硬件周期逻辑时，检查分配、锁竞争、阻塞、I/O、日志和最坏耗时；没有测量时不得声称硬实时。

## 模块和接口

- `[MUST]` 一个模块围绕一个稳定职责组织；按“一个原因改变”判断是否拆分，不按固定行数或 public 方法数量机械拆分。
- `[SHOULD]` 使用窄接口和组合，将协议适配、控制决策、算法、硬件 I/O、诊断序列化分开。
- `[MUST]` 跨层调用显式传递必要上下文，例如分支、轴数量、单位、时间和请求 ID；不得从可变全局状态或上一次调用隐式推断。
- `[SHOULD]` 让调用顺序从代码结构中可见：输入验证、状态检查、状态改变、执行、发布结果。
- `[MAY]` 使用 façade、PImpl、工厂或内部 helper 降低依赖；只有它们减少认知负担时才引入。
- `[MUST NOT]` 让一个类同时拥有应用协议、轨迹规划、硬件驱动细节和日志格式化等互不相关的职责。

## 可观测性和日志

- `[MUST]` 库代码通过统一日志出口记录事件，不在各模块散落使用 `printf`、`cout` 或 `cerr`。
- `[MAY]` 测试和命令行工具使用标准输出；这不等同于运行库日志规范的例外。
- `[MUST]` 日志至少包含等级、模块标签和可读消息；生产环境可以关闭或替换 sink。
- `[MUST NOT]` 在控制周期中执行同步日志、字符串格式化、flush 或不可测量的日志回调。
- `[MUST]` 将固定大小的实时快照与非实时详细诊断分开；不得把 `std::string` 诊断对象直接放入实时共享结构。
- `[SHOULD]` 复用模块已有的错误码、阶段名和诊断映射，不为 UI、Server、Core 和 Runtime 各自维护重复名称表。

## 自查和交付

完成非平凡代码变更后，检查：

- 所有权、生命周期和线程所有权是否明确；
- 状态是否由唯一模块拥有，互斥状态是否使用枚举；
- 失败后是否进入可判断且安全的状态；
- 控制周期是否引入分配、锁、阻塞、I/O、异常或日志；
- 是否复用已有公共类型、诊断和命名约定；
- 是否添加了覆盖失败和边界行为的测试；
- 是否实际运行了相关构建、测试和文档检查。

交付时简要说明行为变化、修改入口、实际验证命令、未验证限制和需要后续演化的规则。简单修改不输出冗长的形式化清单。

